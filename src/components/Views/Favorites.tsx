import React from "react";
import { Container } from "@material-ui/core";

const Favorites = () => {
  return (
    <Container
      style={{
        marginTop: "4em",
        minHeight: "100vh",
        color: "#fff"
      }}
    >
      <h1>Favorites</h1>
    </Container>
  );
};

export default Favorites;
