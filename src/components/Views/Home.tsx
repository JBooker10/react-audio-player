import React from "react";
import { Container } from "@material-ui/core";

const Home = () => {
  return (
    <Container
      style={{
        marginTop: "4em",
        minHeight: "100vh",
        color: "#fff"
      }}
    >
      <h1>Home</h1>
    </Container>
  );
};

export default Home;
