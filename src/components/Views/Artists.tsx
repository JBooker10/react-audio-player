import React from "react";
import { Container } from "@material-ui/core";

const Artists = () => {
  return (
    <Container
      style={{
        marginTop: "4em",
        minHeight: "100vh",
        color: "#fff"
      }}
    >
      <h1>Artists</h1>
    </Container>
  );
};

export default Artists;
